<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/station_card_height"
    android:layout_margin="@dimen/margin_small"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    app:cardBackgroundColor="@color/background_card">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/padding_medium">

        <!-- Station Icon -->
        <ImageView
            android:id="@+id/ivStationIcon"
            android:layout_width="@dimen/icon_size_large"
            android:layout_height="@dimen/icon_size_large"
            android:src="@drawable/ic_ev_station"
            android:background="@drawable/circle_background"
            android:padding="@dimen/padding_small"
            android:contentDescription="@string/station_name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Station Name -->
        <TextView
            android:id="@+id/tvStationName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_medium"
            android:textSize="@dimen/text_size_body_large"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Green Valley Charging Station"
            app:layout_constraintTop_toTopOf="@+id/ivStationIcon"
            app:layout_constraintStart_toEndOf="@+id/ivStationIcon"
            app:layout_constraintEnd_toStartOf="@+id/tvDistance" />

        <!-- Distance -->
        <TextView
            android:id="@+id/tvDistance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_small"
            android:textColor="@color/text_secondary"
            android:background="@drawable/distance_background"
            android:padding="@dimen/padding_small"
            tools:text="2.5 km"
            app:layout_constraintTop_toTopOf="@+id/ivStationIcon"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Address -->
        <TextView
            android:id="@+id/tvAddress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_medium"
            android:layout_marginTop="@dimen/margin_tiny"
            android:textSize="@dimen/text_size_small"
            android:textColor="@color/text_secondary"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="123 Green Valley Road, Colombo 03"
            app:layout_constraintTop_toBottomOf="@+id/tvStationName"
            app:layout_constraintStart_toEndOf="@+id/ivStationIcon"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Status and Availability Row -->
        <LinearLayout
            android:id="@+id/layoutStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="@dimen/margin_medium"
            app:layout_constraintTop_toBottomOf="@+id/ivStationIcon"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Status Indicator -->
            <View
                android:id="@+id/viewStatusIndicator"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:background="@drawable/circle_shape"
                android:backgroundTint="@color/station_available" />

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                android:textSize="@dimen/text_size_small"
                android:textColor="@color/station_available"
                tools:text="Available" />

            <!-- Spacer -->
            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <!-- Available Slots -->
            <TextView
                android:id="@+id/tvAvailableSlots"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_small"
                android:textColor="@color/text_secondary"
                tools:text="3/5 available" />

            <!-- Connector Type -->
            <TextView
                android:id="@+id/tvConnectorType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_medium"
                android:textSize="@dimen/text_size_small"
                android:textStyle="bold"
                android:textColor="@color/primary_green"
                android:background="@drawable/connector_type_background"
                android:padding="@dimen/padding_tiny"
                tools:text="DC" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>